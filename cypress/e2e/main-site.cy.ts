describe('empty spec', () => {
  // beforeEach('main page', () => {
  //   cy.session('mainPage', () => {
  //     cy.visit('localhost:8080')
  //   })
  // })
  it('passes', () => {
    cy.visit('/')
  })
})